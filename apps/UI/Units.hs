-- | Units for specifying lengths, times, angles... etc.
module UI.Units where

-- TODO: support different units + percentages
-- | A length along one dimension, supporting both absolute and
-- relative units.
type Length = Double
